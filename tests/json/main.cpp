//
// Created by ztk on 2022/4/4.
//

#include <json/value.h>

#include <iostream>

using namespace mycs::json;

int main() {
	ArrayValue ary;
	for (int i = 0; i < 10; ++i) {
		ary.push(False);
	}
	delete (None);
	delete (None);
	delete (False);
	delete (True);
	return 0;
}